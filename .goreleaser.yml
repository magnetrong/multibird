version: 2

project_name: multibird

builds:
  - id: multibird
    main: ./cmd/multibird
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X github.com/magnetrong/multibird/internal/version.Version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  use: github

# Uncomment once the tap repo exists.
# brews:
#   - name: multibird
#     repository:
#       owner: magnetrong
#       name: homebrew-multibird
#     homepage: https://github.com/magnetrong/multibird
#     description: Run multiple NetBird VPN instances simultaneously
#     license: MIT
#     dependencies:
#       - name: netbird
